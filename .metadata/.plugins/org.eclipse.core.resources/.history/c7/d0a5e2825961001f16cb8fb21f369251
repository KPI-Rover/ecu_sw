#include "heartbeat.h"
#include "timers.h"

TimerCallbackFunction_t LedOn(TimerHandle_t xTimer);

void HeartbeatInit(void){
	xTimerCreate(	"Heartbeat LED ON timer",
					normal / portTICK_PERIOD_MS,
					pdTrue,
					1,
					LedOn );
	xTimerCreate(	"Heartbeat LED ON timer",
						normal / portTICK_PERIOD_MS,
						pdTrue,
						1,
						LedOn );
	//create timers
	//start timers in normal mode
};
void HeartbeatModeSwitch(heartbeatMode mode){
	//change timers' periods
};

TimerCallbackFunction_t LedOn(TimerHandle_t xTimer){

}


